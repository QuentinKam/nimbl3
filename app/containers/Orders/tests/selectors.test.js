// import { fromJS } from 'immutable';
// import { makeSelectOrdersDomain } from '../selectors';

// const selector = makeSelectOrdersDomain();

describe('makeSelectOrdersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
